#ifndef MYSHELL_H
#define MYSHELL_H

// Function prototypes
void execute_command(char *input);  // Executes a command
void print_working_directory();  // Prints the current working directory
void change_directory(char *path);  // Changes the current directory

#endif
